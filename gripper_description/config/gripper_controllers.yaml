controller_manager:
  ros__parameters:
    update_rate: 100

    gripper_controller:
      type: position_controllers/JointGroupPositionController

gripper_controller:
  ros__parameters:
    joints:
      - left_finger_joint
      - right_finger_joint